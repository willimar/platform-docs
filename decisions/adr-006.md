# Causa raiz finalmente exposta! 🎯

O debug mostrou a arma do crime:

```
[DEBUG] FALHOU: ModuleNotFoundError
[DEBUG] tools.__file__: F:\ai-platform\google-calendar-agent\tools\__init__.py
```

E num run anterior: `Já está no sys.path: F:\ai-platform\youtube-publisher-agent` **antes** de adicionarmos qualquer coisa.

## O que está acontecendo

Os **dois** agentes são membros do workspace → os dois são instalados como **pacotes editáveis** no venv → o uv coloca a raiz de **cada um** no `sys.path` via `.pth` (ordem alfabética: `google-calendar-agent` **antes** de `youtube-publisher-agent`).

Então, quando o Python resolve `import tools`, ele varre o `sys.path` e acha **primeiro** o `tools/` do google-calendar-agent — que não tem `youtube_client.py`. Seu debug manual funcionou justamente porque você fez `sys.path.insert(0, ...)` — posição 0 vence.

**A correção é exatamente essa:** no `load_from_directory`, o pai do agente atual tem que **sempre** ir pra posição 0, mesmo que já esteja no path.

---

## `registry.py` — versão final, limpa (sem prints de debug)

Substitua o `load_from_directory` por:

```python
    def load_from_directory(self, directory: Path) -> int:
        """Carrega todos os módulos .py de um diretório como pacote.

        Adiciona o diretório-pai ao sys.path para que imports internos
        do agente (ex: from tools.auth import ...) funcionem.

        Como múltiplos agentes podem expor pacotes com o mesmo nome
        (ex: "tools/") via installs editáveis, o diretório-pai do agente
        atual é sempre movido para a POSIÇÃO 0 do sys.path, e o cache de
        módulos homônimos é limpo antes da importação.

        Args:
            directory: Diretório contendo módulos com ferramentas.

        Returns:
            Número total de ferramentas novas registradas.
        """
        directory = Path(directory).resolve()
        parent = directory.parent

        if not directory.exists():
            logger.warning("diretorio_nao_existe", diretorio=str(directory))
            return 0

        # Posição 0 vence a busca no sys.path — mesmo que o pai já esteja
        # listado (via .pth de install editável de outro agente homônimo).
        if str(parent) in sys.path:
            sys.path.remove(str(parent))
        sys.path.insert(0, str(parent))

        # Limpa módulos cacheados com o mesmo nome de pacote
        # (conflito entre agentes no mesmo processo).
        nome_pacote = directory.name
        chaves_a_remover = [
            k
            for k in sys.modules
            if k == nome_pacote or k.startswith(f"{nome_pacote}.")
        ]
        for k in chaves_a_remover:
            logger.debug("removendo_modulo_cacheado", modulo=k)
            del sys.modules[k]
        importlib.invalidate_caches()

        count = 0
        try:
            for path in sorted(directory.glob("*.py")):
                if path.name.startswith("_"):
                    continue
                module_name = f"{directory.name}.{path.stem}"
                logger.info("carregando_modulo", modulo=module_name, path=str(path))
                importlib.import_module(module_name)
                count += self.register_from_global()
        finally:
            sys.path.remove(str(parent))

        return count
```

---

## Testar, commitar, pushar

```powershell
cd F:\ai-platform\platform-core
uv run python -m pytest tests/ -q
uv run ruff check --fix src tests
uv run ruff check src tests

git add src/platform_core/tools/registry.py
git commit -m "fix(core): tools_dir do agente atual vence sys.path (conflito de pacotes homonimos)"
git push
```

---

## O run que fecha a F4

```powershell
cd F:\ai-platform
uv run platform run youtube-publisher-agent/agents/youtube-readonly.yml --verbose
```

Agora esperado:

```
[OK] 4 ferramenta(s) carregada(s)
...loop com Ollama...
────────────── Resultado Final ──────────────
Canal: Willimar Augusto Rocha
Playlists: ...
```

> Contingência: se aparecer `connection refused` no Ollama, garanta `ollama serve` rodando e `ollama list` contendo `llama3.1:8b`.

---

## ADR-006 — registrar essa decisão

Título: *"Prioridade de sys.path no carregamento de ferramentas de agentes"*. Contexto: agentes como pacotes editáveis expõem pacotes homônimos (`tools/`); a ordem alfabética dos `.pth` fazia o agente errado vencer o import. Decisão: `load_from_directory` move o pai do agente atual para a posição 0 e limpa `sys.modules` homônimo. Alternativa futura (v0.2): nomespacar os pacotes de tools por agente (`youtube_publisher_tools/`), eliminando a colisão na origem.

---

Me manda o output do run — com o painel verde, a **F4 fecha** e a v0.1 vira milestone oficial no seu `v0.1.md`! 🚀